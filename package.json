{
  "name": "zoom-clone-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "^1.2.3",
    "twilio": "^5.0.0-rc.1"
  },
  "devDependencies": {
    "twilio-run": "^2.5.0"
  },
  "engines": {
    "node": "18"
  }
}
